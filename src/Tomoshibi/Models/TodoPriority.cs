namespace Tomoshibi.Models;

/// <summary>Priority for backlog items. Normal is the quiet default — only
/// high and low get a chip on the row.</summary>
public enum TodoPriority
{
    Low,
    Normal,
    High
}
